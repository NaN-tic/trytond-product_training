<?xml version="1.0"?>
<!-- This file is part product_training module for Tryton.
The COPYRIGHT file at the top level of this repository contains the full copyright notices and license terms. -->
<data>
    <xpath expr="/form/separator[@name='description']" 
            position="before">
        <field name="training" invisible="1"/>
        <group colspan="6"
            states="{'invisible': Not(Bool(Eval('_parent_template', {}).get('training', Eval('training'))))}"
            id="training">
            <separator name="training" colspan="6"/>
            <label name="training_start_date"/>
            <field name="training_start_date"/>
            <label name="training_end_date"/>
            <field name="training_end_date"/>
            <label name="training_registration"/>
            <field name="training_registration"/>
        </group>
    </xpath>
</data>

